export const inputContainerStyle = () => {
  const base = ['flex', 'text-black'];
  return [...base].join(' ');
};

export const inputStyle = () => {
  const base = ['px-2', 'rounded'];
  return [...base].join(' ');
};

export const inputErrorStyle = () => {
  const base = ['h-4', 'text-red-600'];
  return [...base].join(' ');
};
