export const amenityContainerStyle = () => {
  const base = ['flex flex-col', 'mt-2'];
  return base.join(' ');
};

export const amenityTitleContainerStyle = () => {
  const base = ['flex', 'items-center'];
  return base.join(' ');
};
