export const spotFormContainerStyle = () => {
  const base = ['flex flex-col', 'items-center'];
  return base.join(' ');
};

export const spotFormInputsContainerStyle = () => {
  const base = ['flex flex-col', 'w-full md:w-screen-1/2'];
  return base.join(' ');
};
