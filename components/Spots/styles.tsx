export const spotsContainerStyle = () => {
  const base = ['flex'];
  return base.join(' ');
};

export const spotDetailContainerStyle = () => {
  const base = ['flex flex-col', 'w-96', 'bg-red-400'];
  return base.join(' ');
};
