export const addressContainerStyle = (styles) => {
  const base = ['flex', styles];
  return base.join(' ');
};

export const addressDetailsContainerStyle = () => {
  const base = ['ml-2'];
  return base.join(' ');
};
