export const containerStyle = () => {
  const base = ['bg-footer', 'h-14', 'drop-shadow-lg', 'flex'];
  return base.join(' ');
};

export const contentContainerStyle = () => {
  const base = ['flex', 'items-center', 'justify-center', 'w-full'];
  return base.join(' ');
};
